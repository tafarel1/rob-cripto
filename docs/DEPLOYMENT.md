# Deployment

- GitHub Actions: CI (`.github/workflows/ci.yml`, `quality.yml`).
- CodeQL: análise estática de segurança (`codeql.yml`).
- Dependabot: atualizações automáticas (`.github/dependabot.yml`).
- Tags: `git tag v1.1.0` e releases semânticas.
