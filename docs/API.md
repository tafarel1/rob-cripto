# API

- `GET /api/health` — status do backend
- `GET /api/exchange/ticker?symbol=BTC/USDT` — ticker em tempo real
- `GET /api/analysis/smc?symbol=BTC/USDT&timeframe=1h` — análise SMC

Autenticação: via ambiente e chaves do provedor quando aplicável.
